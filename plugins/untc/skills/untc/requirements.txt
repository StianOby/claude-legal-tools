# pypdf is the preferred extractor: on the MTDSG status documents it gives
# one clean row per participant and readable reservation text, where
# pdftotext interleaves the dot leaders with the dates. pdftotext
# (poppler-utils) is used only as a fallback when pypdf is missing.
pypdf>=3.0
