# PDF extraction. The skill prefers `pdftotext` from poppler-utils
# (apt: poppler-utils, brew: poppler) for column-aware output. Install
# pypdf only if poppler isn't available on this machine.
pypdf>=3.0
